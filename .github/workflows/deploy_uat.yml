name: Test SSH Access

on:
  workflow_dispatch:  # allows manual trigger from GitHub UI

jobs:
  test-ssh:
    runs-on: ubuntu-latest
    steps:
      - name: Test SSH Port
        run: |
          nc -zv your-server-ip 22 || echo "SSH port unreachable"
